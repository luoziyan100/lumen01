# Citation Tools + Multistep Reasoning 完成记录

Status: DONE
Audience: reviewer
Action: review-only
Supersedes: active/2026-05-07.01.active.brief.citation-tools-and-multistep-reasoning.md
Next: review 后归档或移动到 done/

## 完成内容

- Rust 后端新增 `search_paper_citations` 和 `search_paper_references` Tauri 命令，调用 Semantic Scholar citations/references API，并复用 `SearchResponse`。
- 复用 `SEMANTIC_SCHOLAR_API_KEY` header 逻辑；`paper_id` 支持 Semantic Scholar 原生格式，limit 默认 20，最大 50。
- `src/services/search.ts` 新增 `searchCitations()` 和 `searchReferences()`，Tauri 与 dev server fallback 都支持。
- Vite dev server 新增 `/api/paper-citations` 和 `/api/paper-references`。
- Agent 工具从 3 个扩展到 5 个：新增 `search_citations` 和 `search_references`。
- 新增 `src/agent/tools/citation-search.ts`，57 行，格式化引用链结果，不做排序或额外参数扩展。
- `executor.ts` 注册两个新工具，不改 `loop.ts`。
- `system-prompt.ts` 重写为多步推理引导，包含搜索自检、引用链导航、证据等级守卫和 5 次工具上限。
- `tests/agent-loop.test.ts` 更新工具列表测试，新增 scenario F 和 citation 工具格式化测试。

## 验证

```bash
cargo check
# Finished dev profile

npm run build
# exit 0

npm run lint
# exit 0

npm run test:agent-loop
# pass 19, fail 0

npm run test:codex
# pass 9, fail 0

npm run test:markdown
# pass 6, fail 0

npm run test:history-window
# pass 4, fail 0

node -e "...RESEARCH_TOOLS length and citation tools assertions..."
# OK: 5 tools

node -e "...buildSystemPrompt citation/multistep assertions..."
# OK: prompt updated

wc -l src/agent/tools/citation-search.ts
# 57 src/agent/tools/citation-search.ts

git diff --check
# exit 0
```

## Review 关注点

- 没有新增 `extract_claims`、`compare_papers` 等推理类工具；这些仍由模型在读取证据后完成。
- 没有修改 `loop.ts`，10 步上限保持不变；prompt 将单轮工具调用上限引导为 5 次。
- `src-tauri/src/commands/citations.rs` 未改动，它仍只负责本地 Graph 引用存储。
